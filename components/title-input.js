import { Input } from 'semantic-ui-react'

export default function TitleInput() {
    return (
        <Input
            style={{ width: '100%' }}
            icon="question"
            placeholder='Title' />
    )
}